Title: Inclusive sum skips the end value

The helper that sums integers in a closed interval is missing the final value.
Expected behavior: sum_inclusive(2, 4) should return 9 because it includes both 2 and 4.
Please keep the existing public API.
