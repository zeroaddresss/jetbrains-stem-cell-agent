Title: Default sort order should be ascending

The helper should sort in ascending order by default.
Expected behavior: sort_numbers([3, 1, 2]) should return [1, 2, 3], while callers can still request descending order explicitly.
Please preserve the public API.
