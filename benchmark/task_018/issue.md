Title: Positive-item helper should work with generators too

The helper accepts any iterable of integers, not just lists.
Expected behavior: generator inputs should produce the same result as list inputs.
Please preserve the current function signature and return shape.
